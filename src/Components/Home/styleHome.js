import { 
    StyleSheet
 } from 'react-native';
 
 const styleHome = StyleSheet.create({
     poster: {
         width: 150,
         height: 250,
         alignItems: "center",
         justifyContent: "center",
     }

 })

 export default styleHome